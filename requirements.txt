# GenAI Autonomous Robotics Agent - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
openai>=1.0.0
numpy>=1.21.0
scipy>=1.7.0

# ROS 2 Python client (install via apt or conda, not pip)
# sudo apt install ros-humble-rclpy ros-humble-nav2-msgs

# Optional: for visualization
matplotlib>=3.4.0
pandas>=1.3.0

# Optional: for testing without ROS
rclpy-mock>=0.1.0  # mock ROS 2

# Development tools
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
